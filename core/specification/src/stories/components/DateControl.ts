import { ComponentControlDate } from '@component-controls/specification';
import { AsReactComponent } from './types';

export const DateControl: AsReactComponent<ComponentControlDate> = (
  _props: ComponentControlDate,
) => null;
