import { ComponentControlButton } from '@component-controls/specification';
import { AsReactComponent } from './types';

export const ButtonControl: AsReactComponent<ComponentControlButton> = (
  _props: ComponentControlButton,
) => null;
