import { ComponentControlArray } from '../../controls';
import { AsReactComponent } from './types';

export const ArrayControl: AsReactComponent<ComponentControlArray> = (
  _props: ComponentControlArray,
) => null;
