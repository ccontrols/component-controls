import { ComponentControlFiles } from '../../controls';
import { AsReactComponent } from './types';

export const FilesControl: AsReactComponent<ComponentControlFiles> = (
  _props: ComponentControlFiles,
) => null;
