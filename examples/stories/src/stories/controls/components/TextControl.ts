import { ComponentControlText } from '@component-controls/specification';
import { AsReactComponent } from './types';

export const TextControl: AsReactComponent<ComponentControlText> = (
  _props: ComponentControlText,
) => null;
