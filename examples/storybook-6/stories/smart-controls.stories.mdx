import { Meta, PropsTable, Playground as Preview, Story, ComponentSource, StorySource } from '@component-controls/storybook';
import { Button } from '../../stories/src/components/Button';

<Meta title="MDX/Smart controls" component={Button} />


# Smart controls
<ComponentSource of={Button} title='Component source' />

<PropsTable of={Button} />


<Preview>
  <Story name="smart story">
    {(props) => (
      <Button {...props}>default</Button>
    )}  
  </Story>
</Preview>


# Small story with custom controls

<Preview>
  <Story name="small story" 
    controls={{
      text: {type: 'text', value: 'Hello'},
    }}
    >
    {({ text }) => (
      <Button>{text}</Button>
    )}  
  </Story>
</Preview>


# Story with no parameters == no smart controls

<Preview>
  <Story name="no controls">
      <Button>Hello</Button>
      <Button>Second button</Button>
  </Story>
</Preview>
